---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app signin-ui
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: signin-ui
  values:
    controllers:
      signin-ui:
        containers:
          app:
            image:
              repository: ghcr.io/woodleighschool/papercut-checkin
              tag: 0.3.4@sha256:8eb3ef147b0594bef8e8b11c763cfa3dc2b8132cbc11071d3e4972c222b14efb
            env:
              TZ: Australia/Melbourne
              SIGNIN_AREAS: |-
                Wellbeing
                Learning Enhancement
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: &port 8080
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: *port
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.woodleighschool.net"
        parentRefs:
          - name: envoy-internal-public
            namespace: network
    persistence:
      config:
        existingClaim: *app
      tmpfs:
        type: emptyDir
        globalMounts:
          - path: /tmp
            subPath: tmp
          - path: /app/instance
            subPath: instance
