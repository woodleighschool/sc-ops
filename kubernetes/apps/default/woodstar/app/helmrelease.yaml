---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: woodstar
spec:
  chartRef:
    kind: OCIRepository
    name: woodstar
  interval: 1h
  values:
    controllers:
      woodstar:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/woodleighschool/woodstar
              tag: 0.1.5@sha256:e9560559d23821329a8d843a8d9f930e9340483609682a48465b4668da985577
            env:
              TZ: Australia/Melbourne
              WOODSTAR_ENTRA_SYNC_INTERVAL: 1h
              WOODSTAR_ENTRA_TRANSITIVE_GROUPS: true
              WOODSTAR_HOST: 0.0.0.0
              WOODSTAR_LOG_LEVEL: debug
              WOODSTAR_MUNKI_S3_BUCKET: woodstar
              WOODSTAR_MUNKI_S3_ENDPOINT: https://67ccf561cd3d68abc81838e145a046b8.r2.cloudflarestorage.com
              WOODSTAR_MUNKI_S3_PATH_STYLE: "true"
              WOODSTAR_MUNKI_S3_PRESIGN_TTL: 15m
              WOODSTAR_MUNKI_S3_PUBLIC_ENDPOINT: https://67ccf561cd3d68abc81838e145a046b8.r2.cloudflarestorage.com
              WOODSTAR_MUNKI_S3_REGION: auto
              WOODSTAR_OIDC_EMAIL_CLAIM: email
              WOODSTAR_OIDC_SCOPES: openid,email,profile
              WOODSTAR_PORT: &port 8080
              WOODSTAR_PUBLIC_URL: "https://{{ .Release.Name }}.woodleigh.vic.edu.au"
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: *port
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /readyz
                    port: *port
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 512Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
    service:
      app:
        ports:
          http:
            port: *port
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.woodleigh.vic.edu.au"
        parentRefs:
          - name: envoy-external
            namespace: network
    persistence:
      tmp:
        type: emptyDir
